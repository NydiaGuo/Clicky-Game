import React from "react";
import "./Header.css";

const Header = () => (
	<header className="header text-center">
		<h1>Clicky Game!</h1>
		<h3 className="text-dark">Click on an image to earn points, but don't click on any more than once!</h3>
	</header>

);


export default Header;